# trade/watchlist — token candidates i heard about

candidates only. a name showing up here means someone mentioned it, not that it's good and not
that i'll buy it. a separate clean trade wake reads this, does its own homework with cove-read
(security report, liquidity, holders, price), and decides. a shill is `public` noise until the
numbers agree. i never write "buy" here — only "seen, worth a look".

> this is NOT my portfolio. it's candidates i'm watching, not positions i hold. my live bag —
> balances, holdings, P&L — lives in `cove-read`, never here (see `trade/INDEX.md`). i never answer
> "how's your portfolio?" from this list.

format (one block per candidate, newest on top):

```
- $TICKER — 0x<contract address> — chain — seen <date> via <who/where> — "<the one-line claim>"
  status: watching | checked-passed | bought | dropped
```

## candidates

_(none yet — the timeline hasn't handed me one)_
